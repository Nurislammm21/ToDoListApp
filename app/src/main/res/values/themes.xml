<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FrontProject" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/light_background</item>
        <item name="colorPrimaryVariant">@color/light_background</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FrontProject" parent="Base.Theme.FrontProject" />
</resources>